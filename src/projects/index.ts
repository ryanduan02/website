import type { Project } from "../types/projects";

import { raspberrypiMetrics } from "./raspberry-pi-metrics";

export const projects: Project[] = [
  raspberrypiMetrics,
  // Add more projects here
];
